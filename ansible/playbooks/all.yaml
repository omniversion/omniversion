---
- name: Fetch all info from servers
  hosts: webservers
  roles:
    - ../roles/check
    - ../roles/autodetect
    - ../roles/refresh
    - ../roles/list
    - ../roles/outdated
    - ../roles/audit
    - ../roles/version
