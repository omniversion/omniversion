---
galaxy_info:
  role_name: outdated
  description: Run the `outdated` command for each package manager on each host. Available for `apt`, `gem`, `go`, `npm`, and `pip`.
  author: Roman Priebe
  company: Layer9 GmbH
  platforms:
    - name: EL
      versions:
        - all
    - name: Debian
      versions:
        - all
    - name: macOS
      versions:
        - all
    - name: MacOSX
      versions:
        - all
    - name: Ubuntu
      versions:
        - all
  min_ansible_version: 2.5
